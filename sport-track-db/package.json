{
  "name": "sport-track-db",
  "version": "0.1.0",
  "description": "database for SportTrack Web Application",
  "main": "sport-track-db.js",
  "scripts": {
    "create-db": "sqlite3 sport_track.db < create_db.sql",
    "reset-db": "rm sport_track.db && npm run create-db",
    "test": "npm run reset-db && node sport-track-db-test.js"
},
  "author": "GUERNY Baptiste - LeCHANU Simon",
  "license": "ISC",
  "dependencies": {
    "sqlite3": "^5.1.6"
  }
}
